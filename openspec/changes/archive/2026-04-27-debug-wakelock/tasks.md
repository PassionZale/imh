## 1. 依赖安装

- [x] 1.1 添加 `wakelock_plus` 依赖到项目

## 2. 代码实现

- [x] 2.1 在 `main.dart` 中导入 `wakelock_plus` 和 `foundation.dart`
- [x] 2.2 在 `main()` 函数中添加 `kDebugMode` 守卫的 WakeLock 启用逻辑

## 3. 验证

- [x] 3.1 以 Debug 模式运行，确认屏幕保持常亮
- [x] 3.2 确认 Hot Reload 在息屏超时后仍然正常工作
